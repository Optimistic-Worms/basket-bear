const db = require('../../db/db-config');

// build an end point that creates a new list in //the DB


exports.saveNotificationsListToDB = (req, res) => {
   
   console.log('saveNotificationsListToDB Working')
}

// convert the end point to be function.